1. First, log in using the `bs_thesis` user account.

2. Open the terminal and go to the `thesis` folder where the `.sh` files are stored.

3. Run `1.sh` first:

```bash id="u4k1pn"
cd ~/thesis or open at desktop

chmod +x ./1.sh
sudo ./1.sh
```

4. After `1.sh` finishes successfully, run `2.sh`:

```bash id="ny8z3v"
chmod +x ./2.sh
sudo ./2.sh
```

5. Restart the system if required after both scripts are completed.
